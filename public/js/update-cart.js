const updateCartButton = document.querySelector("#update-cart");

updateCartButton.addEventListener('click', event => {
    const tbodyTable = document.querySelector('tbody');
    const trow = tbodyTable.querySelectorAll('tr');;
    
})